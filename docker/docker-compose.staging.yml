version: "3.8"
services:
  My-app:
    image: ${IMAGE}:${TAG}
    container_name: My-app-staging
    environment:
      - SPRING_PROFILES_ACTIVE=staging
    ports: ["8081:8081"]
    restart: unless-stopped
